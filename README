To seed initial project directory
---------------------------------
Create a .gitignore with node_modules
npm init
npm install express --save
create index.js
git init
git add *
git add .gitignore
git commit

Can use "git status" to check status of current directory, 
When making any changes:
	git add <filename>
	git commit

Can use "git log" to list versions
Can use "git checkout <unique portion of character code>" to check out a particular version, or "git checkout HEAD" to checkout the latest version.


To deploy in another directory
------------------------------
mkdir <some new directory>
cd <some new directory>
git_clone <original repo> .
npm install

